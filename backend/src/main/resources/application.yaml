spring:
  application:
    name: candestore

  datasource:
    url: jdbc:mysql://mysql.railway.internal:3306/railway?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: UKCLSsnSkgPogEmRRIQoARAbPNdIuVlZ
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  jwt:
    secret: ${JWT_SECRET}